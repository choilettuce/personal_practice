var name = 'k8805'
var letter = 'Dear' + name + ' I also need to validate that each of the strings i.e str1 or str2 or str3 or str4 should not exceed, say, 15 characters. javascript string I have updated this answer. I like the idea of using a match better, but it is sl'

var letter = `Dear ${name} I also need to validate that each of the strings i.e str1 or str2 or str3 or str4 should not exceed, say, 15 characters. javascript string I have updated this answer. 
I like the idea of using a match better, but it is sl`

console.log(letter);

// 숫자 1옆에 ` 기호를 사용하면 띄어쓰기 및 ${}로 변수 사용 가능!!

